# Backend Environment Variables - Copy to .env and update values

# Server Configuration
PORT=5002
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# ⚠️ FOR RENDER PRODUCTION DEPLOYMENT:
# NODE_ENV=production
# CLIENT_URL=https://your-frontend.vercel.app
# PORT is automatically set by Render (usually 10000)

# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://AthleticsMeat:AthleticsMeet@athleticsmeet.2wye07c.mongodb.net/AthleticsMeet2025?retryWrites=true&w=majority&appName=AthleticsMeet

# Gmail Configuration (for sending OTP emails)
GMAIL_USER=your-email@gmail.com
GMAIL_PASSWORD=your-app-specific-password

# JWT Authentication Secret (use a strong random string)
JWT_SECRET=your-secure-jwt-secret-key-here

# OTP Expiry Time (in milliseconds)
OTP_EXPIRY=600000
